import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darthleonard.edaddemiperro',
  appName: 'EdadDeMiPerro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
